services:
  - type: web
    name: ai-file-assistant
    env: docker
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    disk:
      name: data-storage
      mountPath: /app/data
      sizeGB: 1
